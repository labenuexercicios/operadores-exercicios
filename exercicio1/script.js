let par = prompt("insira um num par")
const div = 2
resposta = Number(par)
console.log("answer", par / div, par % div)
// padrão é que só há resultado no resto com números "quebrados"
//se for inserido um número ímpar, a divisão ocorre da mesma forma, mas o resto muda de 0 para 1, significando que o número não é divisível
