let user = (prompt("Digite um numero par!"));
let rest = user % 2
//console.log(rest)
//Quando realiza a operação, ele imprime no console o resto da divisão que sobra por 2 quando setado numero impar.
//Quando inserido numero impar, retorna o resto da divisão, e numero par retorna 0.