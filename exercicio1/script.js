//EXERCICIO 1
const pergNumPar = "Por favor, insira um número par"
const respNumPar = Number(prompt(pergNumPar))

const restoDiv = respNumPar % 2
 
  console.log(restoDiv)

// O padrão encontrada é de que o resto da divisão por 2 sempre será 0, uma vez que os números inseridos sejam pares.
// Quando são inseridos numeros impares, o resto da divisão sempre é 1.

